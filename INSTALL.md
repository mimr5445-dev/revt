# 📦 تثبيت الدفعة v0.4 — العناصر المعمارية

## المحتوى
```
lib/arch/        (7 ملفات جديدة)
components/ArchPanel.tsx   (جديد)
app/page.tsx     (استبدال — يربط اللوحة)
docs/            (توثيق)
```

## المتطلبات السابقة
هذه الدفعة تبني على:
- v0.2 (HistoryControls)
- v0.3 (lib/analysis/* — خصوصاً rooms.ts و measure.ts)
تأكد أنهما موجودان قبل التثبيت.

## الخطوات
1. أضف مجلد `lib/arch/` كاملاً.
2. أضف `components/ArchPanel.tsx`.
3. استبدل `app/page.tsx` بالنسخة هنا (تتضمّن HistoryControls + ProPanel + ArchPanel).
4. Commit + Push:
   ```
   feat(v0.4): العناصر المعمارية — أسقف/درج/أبعاد/فراغات
   ```

## النتيجة
زر **🏛️ معماري** يفتح لوحة بمعاينة مسقطية حية وأربعة تبويبات:
- **الأسقف:** أضف سقفاً (جملون/هرمي/مائل/مسطح) فوق المبنى.
- **الدرج:** حساب تلقائي للدرجات والزاوية.
- **الأبعاد:** توليد تلقائي من الجدران.
- **الفراغات:** تسمية الغرف المكتشفة تلقائياً.

وتصدير الأسقف والدرج إلى OBJ ثلاثي الأبعاد.
